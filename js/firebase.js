import{firebaseConfig}from'./config.js';import{initializeApp}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js';import{getAuth,onAuthStateChanged,signInWithEmailAndPassword,createUserWithEmailAndPassword,signOut}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js';import{getFirestore,doc,setDoc,deleteDoc,getDocs,collection,query,limit}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';
const enabled=Boolean(firebaseConfig.apiKey&&firebaseConfig.projectId);let app,auth,db;if(enabled){app=initializeApp(firebaseConfig);auth=getAuth(app);db=getFirestore(app)}
export{enabled,auth,db,onAuthStateChanged,signInWithEmailAndPassword,createUserWithEmailAndPassword,signOut};export async function pushEntity(uid,type,id,data){if(!enabled)return;await setDoc(doc(db,'users',uid,type,id),{...data,userId:uid},{merge:true})}export async function removeEntity(uid,type,id){if(!enabled)return;await deleteDoc(doc(db,'users',uid,type,id))}export async function pullEntities(uid,type){if(!enabled)return[];const snap=await getDocs(query(collection(db,'users',uid,type),limit(500)));return snap.docs.map(d=>d.data())}
